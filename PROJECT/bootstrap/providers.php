<?php

return [
    App\Providers\AppServiceProvider::class,
    // No other custom service providers were found anywhere in the
    // delivered codebase (no app/Providers directory existed at all).
    // tymon/jwt-auth and laravel/sanctum register their own providers
    // automatically via package discovery, so no entries are needed for
    // them here.
];
